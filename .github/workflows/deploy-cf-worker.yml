name: Deploy Cloudflare Worker

on:
  push:
    branches:
      - main # Or your default branch
    paths:
      - 'cloudflare-worker/**' 
      - '.github/workflows/deploy-cf-worker.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy Worker to Cloudflare
    defaults:
      run:
        working-directory: ./cloudflare-worker

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' 

      - name: Install Wrangler
        run: npm install wrangler # Installs wrangler locally

      - name: Upload GitHub Token Secret for Worker Runtime
        if: ${{ secrets.WORKER_GITHUB_TOKEN }} # Simplified condition
        run: echo "${{ secrets.WORKER_GITHUB_TOKEN }}" | npx wrangler secret put GITHUB_TOKEN --env production
        env:
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}

      - name: Upload Eventbrite Private Token Secret for Worker Runtime
        if: ${{ secrets.EVENTBRITE_PRIVATE_TOKEN }} # Simplified condition
        run: echo "${{ secrets.EVENTBRITE_PRIVATE_TOKEN }}" | npx wrangler secret put EVENTBRITE_PRIVATE_TOKEN --env production
        env:
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}

      # Example for APPS_SCRIPT_SECRET if you add it later
      # - name: Upload Apps Script Secret (Optional)
      #   if: ${{ secrets.YOUR_REPO_SECRET_NAME_FOR_APPS_SCRIPT }} # Simplified condition
      #   run: echo "${{ secrets.YOUR_REPO_SECRET_NAME_FOR_APPS_SCRIPT }}" | npx wrangler secret put APPS_SCRIPT_SECRET --env production
      #   env:
      #     CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
      #     CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}

      - name: Deploy Cloudflare Worker
        run: npx wrangler deploy --env production 
        env:
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
